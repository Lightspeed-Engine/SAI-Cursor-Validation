# Copy to .env.local (gitignored). Used by cursor/scripts for git push — any agent on this machine.
# Fine-grained PAT: repo SAI-Cursor-Validation, Contents read/write. Authorize SSO for Lightspeed-Engine if required.
GITHUB_TOKEN=github_pat_REPLACE_ME

# Optional: identity for local commits when global git user is unset
# GIT_USER_NAME=Lightspeed Engine
# GIT_USER_EMAIL=dev@lightspeed-engine.github.io
