[pytest]
testpaths = tests/shufti
pythonpath = .
python_files = test_*.py
markers =
    offline: no network (pre-commit and PR required gate)
    live: requires Shufti and/or AI-Spy ports
    slice(name): slice id S0-S8
